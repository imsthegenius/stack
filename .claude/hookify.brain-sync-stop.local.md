---
name: brain-sync-stop
enabled: true
event: stop
pattern: .*
action: warn
---

Before finishing, read `~/Desktop/second-brain/.workspace-map.json` to find this project's vault path. Persist any progress, decisions, or learnings to that vault folder's main file (usually `context.md` or `progress.md`). If no mapping exists, write to `~/Desktop/second-brain/learnings/`.
